name: Print message
on:
   push: 
     branches: master
jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - name: Message
          run: echo "Message"
        - name: Alert
          uses: megharajmathur0224/action-send-mail@v3
